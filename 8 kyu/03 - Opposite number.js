// Opposite number
// https://www.codewars.com/kata/opposite-number

// Solution:
// Returns the negation of "number".
function opposite(number) {
  return -number;
};

// This subtracts the number from zero to get the same result.
const opposite = (number) => 0 - number;